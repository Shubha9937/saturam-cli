{
    "name": "sat-cli-credentials-lambda",
    "version": "1.0.0",
    "description": "SAT-CLI remote credential endpoint: API Gateway -> Lambda -> Secrets Manager. Does not call Bedrock.",
    "main": "credentials-handler.js",
    "license": "UNLICENSED",
    "private": true,
    "scripts": {
        "build": "tsc"
    },
    "dependencies": {
        "@aws-sdk/client-secrets-manager": "^3.687.0"
    },
    "devDependencies": {
        "@types/node": "^22.0.0",
        "typescript": "5.9.3"
    }
}
